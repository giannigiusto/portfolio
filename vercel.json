{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": [
          "gem install jekyll sass bundler jekyll-minifier jekyll-sitemap",
          "npm install",
          "npm start"
        ]
      }
    }
  ]
}
